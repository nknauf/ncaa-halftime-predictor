# NCAA Halftime Database (Phase 1)

Phase 1 builds a reproducible historical data pipeline for NCAA men's basketball,
storing games and halftime snapshots in a local SQLite database.

## Setup
```bash
python -m venv .venv
# activate venv...
pip install -r requirements.txt